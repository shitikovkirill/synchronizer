{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Python File",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/cli.py",
            "cwd": "${workspaceFolder}",
            "args": ["source", "replica"]
        }
    ]
}
